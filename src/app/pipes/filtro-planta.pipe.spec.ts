import { FiltroPlantaPipe } from './filtro-planta.pipe';

describe('FiltroPlantaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPlantaPipe();
    expect(pipe).toBeTruthy();
  });
});
