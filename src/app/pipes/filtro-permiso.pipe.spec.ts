import { FiltroPermisoPipe } from './filtro-permiso.pipe';

describe('FiltroPermisoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPermisoPipe();
    expect(pipe).toBeTruthy();
  });
});
